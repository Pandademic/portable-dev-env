version = "0.3.2"

[scripts]
build = [
  "git stash",
  "git pull",
  "docker build -t portable-dev-env ."
]
run = "docker run -it --name pde portable-dev-env /bin/zsh "
fix = [
  echo "attempting every possible fix ... \n May take a while!",
  "git stash",
  "git pull",
  "docker -v",
  "docker rm pde",
  "bonnie build",
  "bonnie run"
]
